// Variable global de JavaScript para administrar el almacenamiento en el Local Storage
localStorage

// Metodos

// Creamos o actualizamos un registro por su llave (key)
localStorage.setItem('key', 'value')

// Consultamos el valor relacionado con una llave (key), si no existe devolverá null
localStorage.getItem('key')

// Elimina o remueve el registro relacionado con una llave (key)
localStorage.removeItem('key')
